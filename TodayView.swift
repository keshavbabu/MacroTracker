import Foundation
import SwiftUI
struct TodayView: View {
    @EnvironmentObject var log: Log
    var body: some View {
        GeometryReader{ geo in
                List{
                    
                }
        }
    }
}
